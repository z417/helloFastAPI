# helloFastAPI 前端编译构建环境变量模板
# 复制本文件为 .env 并在 bun build 前配置以进行环境注入
# 
# 1. 独立后端域名部署 (CORS 跨源访问场景) 示例:
# API_URL=https://api.prod.cinema.com/api
# 
# 2. 生产同源部署 / Nginx 反向代理场景 (相对路径) 示例:
# API_URL=/api
# 
# 3. 本地联调默认场景 (留空或不配置，前端会自动启动智能端口自适应路由，保障开发流畅度):
# API_URL=
